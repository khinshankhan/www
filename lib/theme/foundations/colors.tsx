import { theme as base } from "@chakra-ui/react";

export const colorPalette = {
  ...base.colors,
};

export const colors = {
  ...colorPalette,
  brand: base.colors.purple,
};

export default colors;
