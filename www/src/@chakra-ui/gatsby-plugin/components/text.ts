import { fontSizes } from "constants/fonts";

const Text = {
  baseStyle: {
    fontFamily: `body`,
    fontWeight: `normal`,
    // TODO: look into variants for mono
    fontSize: fontSizes,
  },
};

export default Text;
